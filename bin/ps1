#!/usr/bin/env ruby

vars = {
  :host =>  '\h',
  :cwd => '\w',
  :cwd_basename => '\W',
  :user => '\u'
}

# Git
res = `git branch`
if $?.success?
  puts "YES"
else
  puts "NO"
end

puts "#{vars[:user]}:)"
